import { IframeLoadDirective } from './iframe-load.directive';

describe('IframeLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new IframeLoadDirective();
    expect(directive).toBeTruthy();
  });
});
